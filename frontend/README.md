# Frontend

This directory contains the React frontend for Mohammed Pathariya's portfolio.

## Commands

```bash
npm install
npm start
npm run build
```

The development server runs at `http://localhost:3000`. The production build is generated in `build/` and is disposable. See the [repository README](../README.md) for backend setup, environment variables, deployment, and project documentation.
